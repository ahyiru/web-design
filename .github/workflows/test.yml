name: test

on: workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: npm ci
      run: npm ci
    - name: npm test
      run: npm run lint && npm run test
